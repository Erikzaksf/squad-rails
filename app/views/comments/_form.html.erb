<%= render('layouts/errors', :object => @comment) %>

<%= form_for [@post, @comment] do |f| %>
  <%= f.label :user %>
  <%= f.text_field :user %>
  <%= f.label :reply %>
  <%= f.text_field :reply, size: "100x100" %>
  <p><%= f.submit %></p>
<% end %>
